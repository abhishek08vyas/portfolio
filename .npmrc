# This project lives on a filesystem where pnpm's per-package symlink/junction
# store links fail to materialize (Next's require-hook then throws
# MODULE_NOT_FOUND for react/styled-jsx). node-linker=hoisted builds a flat
# node_modules of real directories (npm-like), which Next resolves cleanly.
node-linker=hoisted
